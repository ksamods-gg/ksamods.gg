/**
 * One shell width for the header and page content. Keeping it in a single
 * place is what stops the header edges drifting away from the page edges.
 */
export const SITE_CONTAINER = 'mx-auto w-full max-w-6xl px-6'
